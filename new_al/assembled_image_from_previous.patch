diff --git "a/algorithm\342\200\214/puzzle_solver/README.md" "b/algorithm\342\200\214/puzzle_solver/README.md"
index 6793b10..222e451 100644
--- "a/algorithm\342\200\214/puzzle_solver/README.md"
+++ "b/algorithm\342\200\214/puzzle_solver/README.md"
@@ -109,11 +109,15 @@ python -m puzzle_solver.vision `
   --config puzzle_solver/examples/vision_config.json `
   --detections detections.json `
   --solution solution.json `
+  --assembled-image assembled-card.jpg `
   --debug-image detection-overlay.png `
   --mask-image detection-mask.png
 ```
 
 Use a camera directly by replacing `--image scene.jpg` with `--camera 0`.
+The assembled image uses the returned rigid transforms to warp the original
+piece pixels into the solved rectangle. The touchscreen UI shows the same image
+beside the detection overlay so the operator can verify the face before motion.
 The default `background_difference` mode estimates the A4 background colour in
 Lab space and keeps all sufficiently different pixels. Unlike a white-only
 threshold, this preserves black and red card printing even when a printed mark
diff --git "a/algorithm\342\200\214/puzzle_solver/tests/test_vision.py" "b/algorithm\342\200\214/puzzle_solver/tests/test_vision.py"
index 748449f..6292535 100644
--- "a/algorithm\342\200\214/puzzle_solver/tests/test_vision.py"
+++ "b/algorithm\342\200\214/puzzle_solver/tests/test_vision.py"
@@ -214,6 +214,7 @@ class VisionTest(unittest.TestCase):
             Calibration,
             VisionConfig,
             detect_pieces,
+            render_assembled_image,
         )
 
         polygons_mm = [
@@ -275,6 +276,10 @@ class VisionTest(unittest.TestCase):
         self.assertAlmostEqual(solved["rectangle"]["width_mm"], 100.0, delta=1.0)
         self.assertAlmostEqual(solved["rectangle"]["height_mm"], 60.0, delta=1.0)
         self.assertLess(solved["metrics"]["hole_ratio"], 0.02)
+        assembled = render_assembled_image(image, detection, solved, calibration)
+        self.assertGreater(assembled.shape[0], 300)
+        self.assertGreater(assembled.shape[1], 500)
+        self.assertGreater(float(assembled.std()), 10.0)
 
 
 if __name__ == "__main__":
diff --git "a/algorithm\342\200\214/puzzle_solver/vision.py" "b/algorithm\342\200\214/puzzle_solver/vision.py"
index 47c713b..eab7e97 100644
--- "a/algorithm\342\200\214/puzzle_solver/vision.py"
+++ "b/algorithm\342\200\214/puzzle_solver/vision.py"
@@ -1054,6 +1054,85 @@ def draw_detection_overlay(
     return overlay
 
 
+def render_assembled_image(
+    corrected_image_bgr: np.ndarray,
+    result: DetectionResult,
+    solution: dict,
+    calibration: Calibration,
+    pixels_per_mm: float = 6.0,
+    margin_mm: float = 4.0,
+) -> np.ndarray:
+    """Warp source-piece pixels into the solved rectangle for visual verification."""
+
+    if corrected_image_bgr is None or corrected_image_bgr.ndim != 3:
+        raise ValueError("corrected_image_bgr must be a colour image")
+    rectangle = solution["rectangle"]
+    origin = np.asarray(rectangle["origin_mm"], dtype=float)
+    width = float(rectangle["width_mm"])
+    height = float(rectangle["height_mm"])
+    if width <= 0.0 or height <= 0.0 or pixels_per_mm <= 0.0:
+        raise ValueError("Assembly dimensions and pixels_per_mm must be positive")
+
+    canvas_width = max(1, int(round((width + 2.0 * margin_mm) * pixels_per_mm)))
+    canvas_height = max(1, int(round((height + 2.0 * margin_mm) * pixels_per_mm)))
+    canvas = np.full((canvas_height, canvas_width, 3), (32, 36, 43), dtype=np.uint8)
+    target_to_canvas = np.asarray(
+        [
+            [pixels_per_mm, 0.0, (margin_mm - origin[0]) * pixels_per_mm],
+            [0.0, pixels_per_mm, (margin_mm - origin[1]) * pixels_per_mm],
+            [0.0, 0.0, 1.0],
+        ],
+        dtype=float,
+    )
+    pieces_by_id = {piece.id: piece for piece in result.pieces}
+    for solved_piece in solution["pieces"]:
+        piece = pieces_by_id.get(solved_piece["id"])
+        if piece is None:
+            continue
+        rotation = np.asarray(solved_piece["rotation_matrix"], dtype=float)
+        translation = np.asarray(solved_piece["translation_mm"], dtype=float)
+        rigid_transform = np.asarray(
+            [
+                [rotation[0, 0], rotation[0, 1], translation[0]],
+                [rotation[1, 0], rotation[1, 1], translation[1]],
+                [0.0, 0.0, 1.0],
+            ],
+            dtype=float,
+        )
+        source_to_canvas = (
+            target_to_canvas
+            @ rigid_transform
+            @ calibration.pixel_to_mm_homography
+        )
+        source_mask = np.zeros(corrected_image_bgr.shape[:2], dtype=np.uint8)
+        cv2.fillPoly(
+            source_mask,
+            [np.rint(piece.contour_px).astype(np.int32)],
+            255,
+        )
+        warped_image = cv2.warpPerspective(
+            corrected_image_bgr,
+            source_to_canvas,
+            (canvas_width, canvas_height),
+            flags=cv2.INTER_LINEAR,
+        )
+        warped_mask = cv2.warpPerspective(
+            source_mask,
+            source_to_canvas,
+            (canvas_width, canvas_height),
+            flags=cv2.INTER_NEAREST,
+        )
+        canvas[warped_mask != 0] = warped_image[warped_mask != 0]
+
+    first = int(round(margin_mm * pixels_per_mm))
+    last = (
+        int(round((margin_mm + width) * pixels_per_mm)),
+        int(round((margin_mm + height) * pixels_per_mm)),
+    )
+    cv2.rectangle(canvas, (first, first), last, (0, 190, 255), 2, cv2.LINE_AA)
+    return canvas
+
+
 def capture_camera(index: int, config: VisionConfig) -> np.ndarray:
     backend = cv2.CAP_DSHOW if os.name == "nt" else cv2.CAP_ANY
     camera = cv2.VideoCapture(index, backend)
@@ -1102,6 +1181,9 @@ def main(argv: Sequence[str] | None = None) -> int:
     parser.add_argument("--solution", type=Path, help="Also solve and write target poses")
     parser.add_argument("--debug-image", type=Path, help="Write contour and pickup overlay")
     parser.add_argument("--mask-image", type=Path, help="Write segmentation mask")
+    parser.add_argument(
+        "--assembled-image", type=Path, help="Write the solved textured assembly"
+    )
     arguments = parser.parse_args(argv)
 
     preliminary = json.loads(arguments.config.read_text(encoding="utf-8"))
@@ -1161,6 +1243,15 @@ def main(argv: Sequence[str] | None = None) -> int:
         arguments.solution.write_text(
             json.dumps(solved, ensure_ascii=False, indent=2) + "\n", encoding="utf-8"
         )
+        if arguments.assembled_image:
+            arguments.assembled_image.parent.mkdir(parents=True, exist_ok=True)
+            assembled = render_assembled_image(
+                corrected_image, result, solved, calibration
+            )
+            if not cv2.imwrite(str(arguments.assembled_image), assembled):
+                raise RuntimeError(
+                    f"Cannot write assembled image: {arguments.assembled_image}"
+                )
     return 0
 
 
diff --git a/viz/recognition_app.py b/viz/recognition_app.py
index ee634eb..e9b6636 100644
--- a/viz/recognition_app.py
+++ b/viz/recognition_app.py
@@ -54,6 +54,7 @@ from puzzle_solver.vision import (  # noqa: E402
     draw_detection_overlay,
     extract_edge_profiles,
     extract_piece_features,
+    render_assembled_image,
 )
 from puzzle_solver.solver import SolverConfig, solve_puzzle  # noqa: E402
 from puzzle_solver.coordinates import A4ToGrblTransform  # noqa: E402
@@ -206,6 +207,7 @@ class RecognitionWorker(QObject):
 
         solution = None
         solve_error = None
+        assembled_image = None
         if self._puzzle_search_enabled:
             try:
                 edge_profiles = extract_edge_profiles(corrected_frame, result.pieces)
@@ -239,6 +241,9 @@ class RecognitionWorker(QObject):
                     target_piece["pickup_target_mm"] = (
                         rotation @ pickup_source + translation
                     ).tolist()
+                assembled_image = render_assembled_image(
+                    corrected_frame, result, solution, calibration
+                )
             except (RuntimeError, ValueError) as exc:
                 solve_error = str(exc)
 
@@ -251,6 +256,7 @@ class RecognitionWorker(QObject):
                 "solution": solution,
                 "solve_error": solve_error,
                 "overlay": overlay,
+                "assembled_image": assembled_image,
                 "puzzle_search_enabled": self._puzzle_search_enabled,
             }
         )
@@ -416,8 +422,15 @@ class RecognitionWindow(QMainWindow):
         layout.setSpacing(16)
 
         self._view = CameraView()
+        self._view.setMinimumSize(640, 480)
         self._view.point_selected.connect(self._add_corner)
-        layout.addWidget(self._view, 1)
+        layout.addWidget(self._view, 2)
+
+        self._assembly_view = CameraView()
+        self._assembly_view.setMinimumSize(320, 240)
+        self._assembly_view.setCursor(Qt.ArrowCursor)
+        self._assembly_view.setVisible(False)
+        layout.addWidget(self._assembly_view, 1)
 
         side = QFrame()
         side.setObjectName("sidebar")
@@ -533,6 +546,7 @@ class RecognitionWindow(QMainWindow):
         self._latest_solution = None
         self._execute_button.setEnabled(False)
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._frozen = False
         self._update_status()
 
@@ -548,6 +562,7 @@ class RecognitionWindow(QMainWindow):
         self._latest_solution = None
         self._execute_button.setEnabled(False)
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._update_status()
 
     def _set_puzzle_search_enabled(self, enabled: bool) -> None:
@@ -562,6 +577,7 @@ class RecognitionWindow(QMainWindow):
         self._latest_solution = None
         self._execute_button.setEnabled(False)
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._update_status()
 
     def _clear_calibration(self) -> None:
@@ -575,6 +591,7 @@ class RecognitionWindow(QMainWindow):
         self._document["screen_a4_corners_px"] = []
         CONFIG_PATH.write_text(json.dumps(self._document, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._update_status()
 
     def _load_red_a4_corners(self) -> None:
@@ -652,6 +669,7 @@ class RecognitionWindow(QMainWindow):
             return
         self._latest_solution = None
         self._execute_button.setEnabled(False)
+        self._assembly_view.setVisible(False)
         frame = self._latest_frame.copy()
         self._recognition_example_path = None
         self._recognition_example_error = None
@@ -714,6 +732,12 @@ class RecognitionWindow(QMainWindow):
             puzzle_search_enabled and solution is not None
         )
         self._view.set_image(payload["overlay"], [])
+        assembled_image = payload.get("assembled_image")
+        if assembled_image is not None:
+            self._assembly_view.set_image(assembled_image, [])
+            self._assembly_view.setVisible(True)
+        else:
+            self._assembly_view.setVisible(False)
         if not puzzle_search_enabled:
             self._status.setText(
                 f"识别到 {len(result.pieces)} 块拼图（拼图搜索已关闭）"
@@ -773,6 +797,7 @@ class RecognitionWindow(QMainWindow):
         )
         self._status.setText(f"识别失败：{error}{suffix}")
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._frozen = False
 
     @Slot()
@@ -888,6 +913,7 @@ class RecognitionWindow(QMainWindow):
         self._latest_solution = None
         self._execute_button.setEnabled(False)
         self._details.clear()
+        self._assembly_view.setVisible(False)
         self._update_status()
 
     def closeEvent(self, event) -> None:  # type: ignore[override]
