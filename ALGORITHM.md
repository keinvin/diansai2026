# 拼图识别与拼接算法说明

本文说明相机识别和拼图求解两个核心算法。系统面向至多四块、每块可用
3--5 个顶点近似的平面拼图碎片，输出以 A4 平面毫米坐标表示。

## 1. 总体流程

相机图像 -> 镜头畸变校正与 A4 坐标标定 -> 前景分割 -> 轮廓提取与碎片几何
拟合 -> 安全吸取点 -> 目标矩形候选 -> 刚体位姿枚举与组合搜索 -> 几何、图案
复核 -> 每块碎片的旋转角、平移量和目标位置。

## 2. 碎片识别算法

### 2.1 坐标标定与识别区域

相机图像首先按已保存的内参进行去畸变。随后使用 A4 纸四角建立从像素
坐标到平面毫米坐标的单应变换：

\[
\begin{bmatrix}X\\Y\\1\end{bmatrix}
\sim
H\begin{bmatrix}u\\v\\1\end{bmatrix}
\]

其中 \((u, v)\) 为图像像素，\((X, Y)\) 为 A4 平面上的毫米坐标，\(H\) 由 A4
角点和其实际尺寸（210 mm x 297 mm）确定。识别只在预先划定的区域内进行，
以避免纸张其他区域干扰。

### 2.2 前景分割

算法在 ROI 内计算灰度中位数 \(g_{bg}\)，以

\[
T = \max(T_{min},\;g_{bg}+\Delta T)
\]

得到自适应亮度阈值。同时在 Lab 色彩空间中计算像素与背景中位色
\(c_{bg}\) 的欧氏距离：

\[
d_{Lab}=\lVert c-c_{bg}\rVert_2
\]

当像素足够亮，或 \(d_{Lab}\) 大于背景差异阈值时，判为碎片前景。后一条件可
保留牌面上的深色、红色印刷，避免图案切断实际物理轮廓。

根据现场光照和背景特征，可采用仅基于 Lab 背景色距离的分割方式，或在 HSV
空间中按低饱和度、高亮度提取白色碎片。

二值掩码再执行小核闭运算和开运算，用于填补小孔、去除孤立噪声，同时尽量
保持直边和角点。

### 2.3 轮廓、分离和多边形拟合

系统从二值掩码提取外轮廓。对于疑似粘连的轮廓，若其直接多边形拟合造成的
面积损失过大，则逐步腐蚀轮廓寻找多个连通种子；再按到各种子的距离，将原
轮廓像素分配为多个碎片。

每个轮廓转换到毫米坐标后，依次进行：

1. 按面积范围、识别区域边界接触情况过滤；
2. 使用 Douglas--Peucker 方法进行轮廓简化；
3. 合并圆角产生的短弦端点，删除近共线或近直线伪顶点；
4. 保留 3--5 顶点的候选多边形；
5. 按中心坐标排序并赋予碎片编号。

若候选超过四块，保留面积最大的四块，其余作为诊断轮廓。这是对竞赛拼图块
数量上限的直接约束。

### 2.4 安全吸取点

对每个完整轮廓生成内部掩码并计算欧氏距离变换，取距离值最大的像素作为
吸取点：

\[
p_{pick}=\arg\max_{p\in\Omega} D(p,\partial\Omega)
\]

它位于碎片内部、且离边界最远；最大距离经单应变换局部尺度换算为毫米，作为
吸取余量。

识别结果包括轮廓、多边形顶点、面积、质心、最短边、吸取点及其余量。

## 3. 拼图求解算法

### 3.1 目标矩形候选

设碎片多边形面积之和为 \(A\)。求解器在给定的宽、高范围和 1 mm 栅格上枚举
矩形 \((W, H)\)，仅保留满足

\[
\frac{|WH-A|}{A}\leq\varepsilon_A
\]

的候选。矩形边长优先由碎片边长的可达和构成；若未找到候选，才执行有限的
面积扫描作为容错回退。候选按面积误差及与偏好长宽比的偏差排序。

### 3.2 刚体位姿枚举

对每块碎片，枚举其每一条边与目标矩形四条外边的贴合方式。每一种方式给出
平面刚体变换：

\[
p' = R(\theta)p+t,
\qquad
R(\theta)=
\begin{bmatrix}
\cos\theta & -\sin\theta\\
\sin\theta & \cos\theta
\end{bmatrix}
\]

其中 \(\theta\) 是碎片旋转角，\(t\) 是平移量。候选会检查是否落在矩形内；每块
碎片至少有一条边占据目标矩形边界，这是搜索空间得以大幅缩小的关键先验。

### 3.3 位掩码组合搜索

每个候选位姿以 1 mm 栅格离散为占用位图。搜索组合时，利用位运算累积各碎片
的占用区域，快速计算：

- 未覆盖格数，即矩形孔洞；
- 被多个碎片同时覆盖的格数，即重叠；
- 每块碎片是否恰好选择一个位姿。

只有孔洞比例和重叠比例不超过预设阈值的组合才进入下一阶段。单个矩形的搜索
节点数与总求解时间均设置上限，以保证实时性。

### 3.4 几何可行性与执行安全

对几何解恢复碎片之间的相邻边，检查对应端点距离并要求所有碎片通过相邻关系
连通。随后按碎片相对矩形中心的方向施加安全间隙；如果仍有重叠，则以小步长
将冲突碎片相向分开，直至重叠面积满足最终容差。

因此输出的目标位置与平移量已包含安全偏移，可直接用于机械执行。

### 3.5 图案与牌面特征复核

几何上可能存在多个合法解时，系统从每条边内侧采样三条 Lab 色彩带，并比较
拼接边反向对齐后的色彩差：

\[
e_{pattern}=\frac{1}{N}\sum_{i=1}^{N}
\frac{\lVert Lab_{a,i}-Lab_{b,i}\rVert_2}{255\sqrt{3}}
\]

颜色变化很小的纯色边缺乏判别力，因此其图案证据权重会降低。系统还可利用
圆角位置、红黑角标密度，以及牌面在 180 度旋转下的红黑图案对称性评分。

最终排序分数是几何分数与图案、圆角、角标和对称性失配的加权和；证据不足时，
相应外观项不会主导结果。

### 3.6 扑克牌花纹的特殊处理

扑克牌碎片的外轮廓可能产生多个几何上可行的拼接方向，因此牌面花纹用于确认
碎片的正反、上下和相邻关系。处理时保留白色牌面上的红色与黑色印刷区域，避免
将花色、数字或字母误分为背景。

花纹复核主要利用以下特征：

1. **切口两侧的图案连续性。** 同一原始牌面被切开后，切口附近的颜色、笔画和
   花色轮廓在正确拼接处应连续。将两条候选拼接边按相反方向对齐后，比较其内侧
   颜色剖面；差异较小的组合更可信。
2. **红黑花色分布。** 红桃、方块对应红色区域，黑桃、梅花对应黑色区域。正确
   拼接后，同一花色的断裂部分应在切口处延续，不应出现明显的红黑错接。
3. **角标位置。** 牌角通常包含点数与花色标记，且靠近圆角。若候选解将这些高
   墨迹区域放在不合理的内部位置，或使其远离牌角，则降低该解的可信度。
4. **180 度旋转对称性。** 标准扑克牌通常在旋转 180 度后仍可正常阅读，牌面中
   心图案及对角角标具有近似中心对称性。将已拼接牌面的红、黑花纹栅格旋转 180
   度后，使用重叠程度衡量对称性：

\[
S_{sym}=\frac{|M\cap R_{180}(M)|}{|M\cup R_{180}(M)|}
\]

其中 \(M\) 是某一颜色花纹的占用区域，\(R_{180}(M)\) 是其旋转结果。\(S_{sym}\)
越接近 1，表示该颜色花纹的对称性越好。

上述特征只用于区分已经通过面积、边界、孔洞、重叠和连通性检验的几何候选。
当牌面印刷过浅、反光严重或切口附近没有花纹时，花纹证据会不足，此时应以几何
约束作为主要判据。

## 4. 算法输出

识别阶段输出每块碎片的轮廓、多边形顶点、面积、质心、最短边、吸取点及吸取
余量。拼图阶段输出目标矩形尺寸，以及每块碎片的旋转角、平移量、目标多边形
和用于避免机械干涉的安全偏移。
